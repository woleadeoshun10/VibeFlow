Create Vite project using react template (npm create vite@latest . -- --template react)
Install axios for api calls
Import useState from react and axios
Import App.css 

Create the api url constant for easy reuse

Create App function
Deconstruct Array to to use the value and set value for - mood,playlist,favorite

create mood Array

create fetch function
1. User clicks "Happy"
2. setLoading(true)  → Spinner appears
3. API call...
   - Success → Show playlists
   - Error → Show alert
4. finally runs
5. setLoading(false) → Spinner disappears

Return JSX

Think of it like
function App() {
  // 1. Set up data (state)
  // 2. Define functions (fetchPlaylists)
  // 3. Return what to show (UI) ← WE'RE HERE!
  return (
    <div>All your beautiful UI</div>
  )
}

onClick
1. User clicks "Happy" button
2. onClick fires
3. setSelectedMood('happy')  → Button glows red
4. fetchPlaylists('happy')   → Fetch playlists
   a. setLoading(true)       → Show spinner
   b. Call backend API
   c. setPlaylists([...])    → Show playlists
   d. setLoading(false)      → Hide spinner

